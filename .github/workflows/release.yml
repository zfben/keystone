name: Release

on:
  push:
    branches:
      - master
      - release-debugging

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master

      - name: Test thing
        run: git log --diff-filter=A --max-count=1 --pretty=format:%h ./.changeset/chilly-berries-yell.md
      - name: Test thing
        run: git log --diff-filter=A --max-count=1 --pretty=format:%h ./.changeset/fuzzy-tables-matter.md
      - name: Test thing
        run: git log --diff-filter=A --max-count=1 --pretty=format:%h ./.changeset/old-geese-vanish.md
      - name: Test thing
        run: git log --diff-filter=A --max-count=1 --pretty=format:%h ./.changeset/tasty-carrots-whisper.md
